<Application x:Class="HomeSocialNetwork.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfHomeNet" StartupUri="MainWindow.xaml"
             xmlns:converters="clr-namespace:WpfHomeNet.Converters"> 
           
    <Application.Resources>
        <converters:RelativeCornerRadiusConverter x:Key="RadiusConverter"/>
        <converters:BorderCornerRadiusConverter x:Key="BorderRadiusConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
       

        <!-- Стиль кнопки, доступный во всём приложении -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="AntiqueWhite"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Height" Value="Auto"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Padding" Value="10"/>


            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"  
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{Binding ActualWidth,
                                RelativeSource={RelativeSource TemplatedParent},
                                Converter={StaticResource RadiusConverter}}"  Padding="{TemplateBinding Padding}">

                            <ContentPresenter x:Name="contentPresenter"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        TextElement.FontWeight="{TemplateBinding FontWeight}"
                                        TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <!-- Триггеры визуальных состояний -->
                        <ControlTemplate.Triggers>
                            <!-- Наведение курсора -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#66ABC8AB"/>
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="Black"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFCCCCCC"/>
                                <Setter TargetName="border" Property="CornerRadius" Value="9"/>
                            </Trigger>
                            <!-- Нажатие -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFD5D5D5"/>
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="#FF000000"/>
                            </Trigger>
                            <!-- Отключённое состояние -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FFF5F5F5"/>
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="#FF999999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BorderedContentStyle" TargetType="ContentControl">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Border BorderThickness="0.15"
                                Margin="1,1,1,0"
                                BorderBrush="Black"
                                Padding="2"
                                CornerRadius="{Binding ActualWidth,
                                            RelativeSource={RelativeSource TemplatedParent},
                                            Converter={StaticResource RadiusConverter}}" 
                         
                                HorizontalAlignment="{TemplateBinding HorizontalAlignment}">
                            <TextBlock Text="{Binding Content, RelativeSource={RelativeSource TemplatedParent}, StringFormat={}{0}}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           FontSize="14"
                           />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TripleBorderStyle" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Grid>
                            <!-- Внешний Border (только рамка, без градиента) -->
                            <Border CornerRadius="{Binding ActualWidth,
                               RelativeSource={RelativeSource TemplatedParent},
                               Converter={StaticResource BorderRadiusConverter},
                               ConverterParameter=0.0184}"
                          BorderThickness="0.6"
                          BorderBrush="Black"
                          Background="White">
                                <!-- Белый фон для всего элемента -->

                                <!-- Контейнер для вложенных Border -->
                                <Grid>
                                    <!-- Средний Border -->
                                    <Border BorderThickness="0.6"
                                    BorderBrush="Silver"
                                    CornerRadius="{Binding ActualWidth,
                                        RelativeSource={RelativeSource TemplatedParent},
                                        Converter={StaticResource BorderRadiusConverter},
                                        ConverterParameter=0.0177}"/>

                                    <!-- Внутренний Border (с градиентом) -->
                                    <Border Margin="0.6"
                                    BorderThickness="0.6"
                                    BorderBrush="Black"
                                    CornerRadius="{Binding ActualWidth,
                                        RelativeSource={RelativeSource TemplatedParent},
                                        Converter={StaticResource BorderRadiusConverter},
                                        ConverterParameter=0.017}">

                                        <!-- Градиентный фон ВНУТРИ внутреннего бордера -->
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <GradientStop Color="#FF668C66" Offset="0.0"/>
                                                <GradientStop Color="Tan" Offset="0.7"/>
                                                <GradientStop Color="#99104710" Offset="1.0"/>
                                            </LinearGradientBrush>
                                        </Border.Background>

                                        <!-- Контент -->
                                        <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>









    </Application.Resources>
</Application>
